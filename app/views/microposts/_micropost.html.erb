<li id="micropost-<%=micropost.id%>">
    <%=link_to gravatar_for(micropost.user,size:50), micropost.user%>
    <span class="user"><%=link_to micropost.user.email, micropost.user%></span>
   <p> <span class="topic">Title:<%=link_to micropost.title, micropost,micropost: micropost%></span>

     <span class="timestamp">
      Posted <%=time_ago_in_words(micropost.created_at)%> ago.
      <%= link_to 'Delete', micropost, method: :delete, data: { confirm: 'Are you sure?' } %>
  </span></p>
  </li>
