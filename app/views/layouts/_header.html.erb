<header class="navbar navbar-fixed-top navbar-inverse">
<div class="container">
<%=link_to"News", root_path,id:"logo"%>
<nav><ul class="nav navbar-nav navbar-right">
<li><%=link_to"Home", root_path%></li>
<li class="dropdown">
<a class="dropdown-toggle" data-toggle="dropdown">News <b class="caret"></b></a>
    <ul class="dropdown-menu" role ="menu">
        <li><%=link_to "A.I", topic_path(:name => "A.I") %></li>
        <li><%=link_to "Game", topic_path(:name=>  "Game")%></li>
        <li><%=link_to "Rmit", topic_path(:name => "Rmit")%></li>
        <li><%=link_to "V.R.", topic_path(:name => "V.R.")%></li>

    </ul>
    </li>
<%if logged_in?%>

<%if current_user.name.nil?%>
<li><%=link_to "#{current_user.email}", current_user %></li>
<%else%>
<li><%=link_to "#{current_user.name}", current_user %></li>
<%end%>
        <%# <li><%= link_to gravatar_for(current_user), :size => 125,logout_path, method: :delete%></li> 
        <li><%=link_to gravatar_for(current_user, size: 40), logout_path,method: :delete,id:"gravatar"%></li>
<%else%>
<li><%=link_to "Log in", login_path%></li>
<%end%>
</ul>
</nav>
</div>
</header>
